{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "dependsOn": [],
            "args": [
                "build",
                "${workspaceFolder}/examples/Greeter.sln"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}